{
  "name": "clawos",
  "version": "0.1.0",
  "description": "Security architecture for autonomous agents",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --test tests/",
    "test:watch": "node --test --watch tests/",
    "lint": "tsc --noEmit"
  },
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
